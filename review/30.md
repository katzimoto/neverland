# PR 30 Review — Atlassian source connectors

## Blockers
- None.

## Warnings
- `pytest tests/unit/test_connectors.py -q` passes all connector tests, but the repository-wide coverage gate fails when running that targeted file alone because `pyproject.toml` enforces 90% coverage across all `src` modules. Re-ran the same target with `--no-cov` to validate behavior without the global coverage gate.

## Suggestions
- Consider adding a follow-up hardening pass that validates absolute attachment URLs remain on the configured Atlassian host before downloading, so a compromised/self-hosted Atlassian response cannot redirect connector workers to arbitrary origins.

## Coverage / Checks
- `python -m ruff check src/services/connectors tests/unit/test_connectors.py` — passed.
- `pytest tests/unit/test_connectors.py -q --no-cov` — passed, 32 tests.
- `mypy src --strict` — passed.

## Verdict
- Approved and merged.
