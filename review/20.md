## Review - PR #20

### Phase 07e: Related Documents And Expertise Map

**Branch:** `developer/phase-07e-related-expertise`
**Reviewer:** Pending

---

### What changed

1. **Related documents**
   - Adds a backend endpoint for permission-filtered related document surfacing.
   - Uses Qdrant chunk vectors, excludes the source document, deduplicates by document,
     and respects `search.related_docs_limit`.

2. **Expertise map**
   - Adds a backend endpoint for neutral topic-based expertise evidence.
   - Scores views, comments, shared annotations, and enabled subscriptions over
     topic-matching accessible documents.
   - Returns aggregate signal counts and accessible document evidence only.

3. **Feature flags and docs**
   - Enforces `feature.related_docs` and `feature.expertise_map`.
   - Updates `CHANGELOG.md`, `agents.md`, and the Phase 07 implementation plan.

---

### Automated checks

- `.venv/bin/ruff check .` — passed
- `.venv/bin/ruff format --check .` — passed
- `.venv/bin/mypy src --strict` — passed
- `.venv/bin/pytest tests/unit -q --no-cov` — passed, 143 tests
- `.venv/bin/pytest tests/test_migrations.py -q --no-cov` — passed, 4 tests
- `.venv/bin/pytest tests/integration/test_related_api.py -q --no-cov` — passed, 4 tests
- Secret scan from `skill.md`, scoped to project files and excluding `.venv` — passed
- `.venv/bin/pip-audit` — blocked by sandbox DNS/network access to `pypi.org`;
  escalated rerun was requested but rejected by the session approval system

---

### Blockers (must fix before merge)

None.

---

### Warnings (should fix)

- Full `pytest` was not completed in this local runner because existing
  TestClient-based API tests stall after Alembic setup. Phase 07e behavior is
  covered with migrated-DB service/repository integration tests to keep this
  PR moving without deep-diving the local ASGI test harness.

---

### Coverage report

Focused Phase 07e integration tests passed without coverage enabled:
`4 passed in 6.70s`.

---

### Checklist

- [x] CHANGELOG.md updated
- [x] agents.md updated for Phase 07e
- [x] No migration required
- [x] No private annotation or comment text returned as expertise evidence
- [x] Lint: 0 errors
- [x] Type check: 0 errors
- [x] Focused tests pass
- [ ] Dependency audit clean (blocked by sandbox network)

---

### Verdict

**Changes pending dependency audit/full CI validation.**
