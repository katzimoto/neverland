## Review - PR #19

### Phase 07d: Subscriptions, Notifications, Alert Matching

**Branch:** `developer/phase-07d-subscriptions-notifications`
**Reviewer:** Codex

---

### What changed

1. **Schema**
   - Adds `alert_subscriptions` and `alert_notifications` with cascade foreign keys,
     notification indexes, and duplicate notification prevention.

2. **API**
   - Adds `GET|POST /subscriptions`.
   - Adds `PUT|DELETE /subscriptions/{subscription_id}`.
   - Adds `GET /notifications` and `PUT /notifications/{notification_id}/read`.
   - Adds `POST /admin/alerts/{documant_id}/trigger`.

3. **Alert matching**
   - Adds `AlertMatcher` using `MockEncoder` cosine similarity.
   - Filters matching candidates through source permissions.
   - Wires best-effort ingest-time matching behind `alerts.check_on_ingest`.

4. **Docs and tests**
   - Updates `CHANGELOG.md`, `agents.md`, and the Phase 07 implementation plan.
   - Adds integration tests for CRUD, feature flags, permission-filtered matching,
     read state, migration shape, and ingest-time matching.

---

### Automated checks

| Check | Result |
|---|---|
| `.venv/bin/ruff check .` | Pass |
| `.venv/bin/ruff format --check .` | Pass |
| `.venv/bin/mypy src --strict` | Pass |
| `.venv/bin/pytest` | Pass - 277 passed, 57 warnings |
| Secret scan | Pass - no matches |
| `.venv/bin/pip-audit` | Pass - no known vulnerabilities found |

---

### Blockers (must fix before merge)

None.

---

### Warnings (should fix)

None.

---

### Coverage report

| Scope | Measured | Threshold | Status |
|---|---|---|---|
| Unit + Integration | 92.96% | 90% | Pass |

---

### Checklist

- [x] All public symbols documented
- [x] CHANGELOG.md updated
- [x] agents.md updated for Phase 07d
- [x] Migration has downgrade path
- [x] FK constraints use ON DELETE CASCADE where ownership should cascade
- [x] Dependency audit clean
- [x] Lint: 0 errors
- [x] Type check: 0 errors
- [x] Tests pass

---

### Verdict

**Approved.**
