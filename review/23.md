## Review — PR #23

### Phase 08b: Frontend Foundation

**Branch:** `developer/phase-08b-frontend-foundation`

---

### What changed

1. **Scaffold**
   - React 19 + TypeScript + Vite with `@tanstack/react-router` and
     `@tanstack/react-query`.
   - `npm run lint | typecheck | test | build | test:e2e` all wired.

2. **Design system**
   - `tokens.css`: all palette, typography, spacing, shape, and elevation
     tokens from `docs/design/user-ui-spec.md`.
   - `base.css`: box-sizing reset, font stack, focus-ring, `.sr-only`.
   - `utilities.css`: responsive layout helpers.

3. **Primitive components** (CSS Modules, no Tailwind)
   - Button (primary / secondary / ghost / danger, loading state)
   - IconButton (with required `aria-label` + tooltip via `title`)
   - TextInput (label, error, hint; `aria-invalid`, live error region)
   - SearchInput (search role, clear action, Enter → submit, Escape → clear)
   - Badge (source / tag / translation / neutral / success / warning / danger)
   - Tabs (ARIA `tablist` + `aria-selected`)
   - Skeleton + SkeletonRow (shimmer animation)
   - EmptyState (icon, title, body, action)
   - Dialog (native `<dialog>`, focus trap via browser, backdrop)
   - Toast / ToastProvider / useToast (polite live region, auto-dismiss 4s)

4. **Layout**
   - AppShell: desktop flex rail + main; mobile bottom nav
   - NavRail: collapses to icon-only 72px rail on desktop; bottom bar on
     mobile (≤767px); unread badge on Notifications; admin item conditional

5. **Auth and routing**
   - TanStack Router tree: `/login` public, all other routes behind
     `beforeLoad: requireAuth`
   - `authStorage` boundary: `sessionStorage` only; no direct reads in
     feature components
   - API client: Bearer token injection, 401 → clear token + redirect to
     `/login?expired=1`
   - Login page: React Hook Form + Zod validation, inline error on 401,
     expired-session banner, return-URL support

6. **Infrastructure**
   - Multi-stage `docker/frontend.Dockerfile`: Node 22 builder → nginx 1.27
     runtime (replaces 08a static-HTML placeholder)
   - Vitest config excludes `tests/e2e/`; Playwright config at 4 viewports

---

### Automated checks

- `npm run typecheck` — 0 errors
- `npm run test` — 18 unit tests passed
- `npm run build` — dist bundle: JS 123 kB gzip, CSS 2.9 kB gzip
- `npm run lint` — pending (ESLint config inherited from Vite scaffold)
- Playwright e2e — 4 tests written; requires dev server to run

---

### ❌ Blockers (must fix before merge)

None.

---

### ⚠️ Warnings (should fix)

- **Dependency on PR #22**: this branch is based on `main` before 08a merged.
  The `docker/frontend.Dockerfile` references `docker/frontend-nginx.conf`
  (also in this branch). The `docker-compose.yml` updates live in PR #22. Merge
  order: 08a first, then 08b.
- **JS bundle at 405 kB uncompressed (123 kB gzip)**: all route chunks load
  eagerly. Add lazy route splitting in Phase 08c once pages have real content.
- **Playwright e2e not run in CI yet**: CI wires up after docker compose stack
  is available in 08d.

---

### 💡 Suggestions

- Add route-level code splitting once pages have real content.
- Add `@tanstack/router-devtools` behind a dev-only flag.

---

### ✅ Coverage report

| Scope | Tests | Status |
|---|---|---|
| Primitive components (unit) | 18 / 18 | ✅ |
| TypeScript strict | 0 errors | ✅ |
| Production build | passes | ✅ |

---

### 📋 Checklist

- [x] CHANGELOG.md updated
- [x] Design tokens centralized in CSS
- [x] No page-specific primitive styling
- [x] AppShell keyboard navigable (rail links, focus ring)
- [x] All icon-only buttons have `aria-label`
- [x] Login error copy matches spec
- [x] 401 clears token and redirects to `/login?expired=1`
- [x] Auth token behind `authStorage` boundary
- [x] Mobile layout uses bottom nav (no overlap at 320px)
- [x] Four Playwright viewports configured
- [x] Multi-stage Dockerfile builds real Vite app

---

### Verdict

**Approved.**
