## Review — PR #13

### ❌ Blockers (must fix before merge)

None.

### ⚠️ Warnings (should fix)

- `docs/implementation/phase-05-preview-enrichment.md` conflict resolved by keeping main's version (05b marked as ✅ Merged / #12). The branch had stale status values (05b as Planned / #11). Resolution is correct.
- `docs/design/document-comments-spec.md:316` — Endpoint table suggests `PATCH` for edits. The implementation plan uses `PATCH` too. Ensure this aligns with existing update patterns in `src/services/api/main.py` (which currently uses `POST` for most mutations). Consider `PUT` or `POST` for consistency unless `PATCH` partial-update semantics are strictly needed.
- `docs/implementation/translation-versions-plan.md` — Backfilling fast versions from existing indexed data is listed as "optional." If deferred, document the exact compatibility behavior (e.g., old documents show `translation_quality = 'fast'` but have no version records until a new manual request is made).

### 💡 Suggestions (nice-to-have)

- Add a `docs/design/README.md` navigation file to match `docs/implementation/README.md` and `docs/review/README.md`.
- Consider adding `feature.document_comments` and `feature.translation_versions` to `shared/feature_flags.py` and `system_config` defaults now so Phase 07 and 05c can toggle them without migration churn.
- The document comments spec mentions "no product-level short character limit" but the implementation plan mentions a "configurable abuse/safety byte limit at the API boundary." Define that limit in `system_config` now (e.g., `comments.max_body_bytes: 65536`).

### ✅ Coverage report

| Scope | Measured | Threshold | Status |
|---|---|---|---|
| Docs | N/A | N/A | N/A |

This PR contains only documentation and specs. No code changes, no tests required.

### 📋 Checklist

- [x] All new docs have consistent headers
- [x] No broken internal links (verified by pattern matching)
- [x] spec.md and spec-v4.pdf unchanged
- [x] No secrets or credentials
- [x] No code changes → no lint/test impact
- [x] Cross-references between design specs and implementation plans are present

### Verdict

**Approved.** This is a documentation-only PR that adds UX specs and implementation plans for two future features (document comments and translation versions). All content is additive, well-structured, and properly cross-referenced. Zero blockers.
