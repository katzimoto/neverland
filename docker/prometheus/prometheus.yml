global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/alerts.yml

scrape_configs:
  - job_name: neverland-api
    metrics_path: /metrics
    scheme: http
    static_configs:
      - targets:
          - api:8000
